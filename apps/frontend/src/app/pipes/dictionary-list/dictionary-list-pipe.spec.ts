import { DictionaryListPipe } from '../dictionary-list-pipe';

describe('DictionaryListPipe', () => {
    it('create an instance', () => {
        const pipe = new DictionaryListPipe();
        expect(pipe).toBeTruthy();
    });
});
